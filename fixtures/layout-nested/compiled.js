(function (template, ctx) {
  let out = ''
  out += 'I will define the header myself'
  out += ''
  out += '\n'
  out += 'Content from super'
  out += ''
  out += '\n'
  out += 'Appended by index'
  return out
})(template, ctx)