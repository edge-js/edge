(function (template, ctx) {
  let out = ''
  out += 'This is the base template'
  out += '\n'
  out += ''
  out += '\n'
  out += 'I will override the base content'
  out += '\n'
  return out
})(template, ctx)